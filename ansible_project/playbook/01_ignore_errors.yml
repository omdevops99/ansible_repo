--- 
 - name: error handling
   become: true
   hosts: localhost
   tasks: 
       - name: this task will fail
         ansible.builtin.command: /bin/false
         ignore_errors: true
       - name:  this still runs inspite of the failure
         debug: 
           msg: "play confinued despite of the failure"